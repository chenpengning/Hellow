<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".work.MyWorkActivity">

    <Button
        android:id="@+id/onTimeWork"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:onClick="onTimeWorkLisenter"
        android:text="一次任务"
        app:layout_constraintBottom_toTopOf="@+id/repeatTimeWork"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/repeatTimeWork"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="611dp"
        android:onClick="onTimeWorkLisenter"
        android:text="多次次任务"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/onTimeWork" />

</androidx.constraintlayout.widget.ConstraintLayout>